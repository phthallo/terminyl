# terminyl
 A CLI Pomodoro Timer in Rust <sub>but because I don't really know Rust, it's really bad</sub>

To use, clone the repository and `cd terminyl`. 
Then run `cargo run <study_time> <rest_time> <session count>` in a terminal window.

